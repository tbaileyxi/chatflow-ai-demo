# Legacy web app subfolder (now iOS-only)
chatflow-ai-demo/

# Claude/Codex worktrees
.claude/worktrees/
.codex/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Secrets — .env was tracked until 2026-08-06; untracked then, keep it that way.
# Its old values are still in git history, so treat those keys as exposed.
.env
.env.*
!.env.example

# iOS build output. Three .ipa files were committed before this rule existed,
# which is most of why the packfiles are ~80MB.
*.ipa
_old_ipas/

# App Store / marketing assets — large binaries, not source
appstore-screenshots/

# Scraper output — 188MB and 42MB respectively. Data, not source; regenerate
# with the scripts rather than committing it.
sponsor-db/
chapter-db/

# Supabase CLI scratch
supabase/.temp/

# Vite writes these while resolving the config; regenerated every run
vite.config.ts.timestamp-*.mjs
